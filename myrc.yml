apiVersion: v1
kind: ReplicationController
metadata:
  name: mynginx-rc
spec:
  replicas: 5
  selector:
    app: mynginxpods
  template:
    metadata:
      labels:
        app: mynginxpods
    spec:
      containers:
        - name: mynginxcontainer
          image: nginx
          ports:
            - containerPort: 80
